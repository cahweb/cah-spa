// This one has no actual logic; it just updates one of
// its HTML values based on the prop, and Athena handles
// the rest.
export default {
    props: {
        value: Number,
    },
    data() {
        return{}
    },
}